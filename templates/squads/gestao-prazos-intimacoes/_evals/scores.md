# Scores — gestao-prazos-intimacoes

> Log de avaliações (`/criminalsquad eval`). Para acompanhar a qualidade e pegar regressão.

| Data | Run/Caso | Nota | Verdict | Observações |
|------|----------|------|---------|-------------|
| 2026-07-26 | smoke-run/exemplo-varredura-djen-carteira-mista | 88 | APROVADO | As 4 datas batem com o gabarito e a armadilha cara foi evitada (prazo cível não entrou no motor penal). .ics e JSON do motor conferidos em disco. Perdeu ponto por não emitir régua do 4º prazo e por autoavaliação miscalibrada (REJECT indevido). |
| 2026-07-27 | smoke-run/exemplo-varredura-djen-carteira-mista | 50 | REPROVADO | 2ª avaliação (a 1ª foi 88 em 26/07 — a queda é de RIGOR DO JUIZ, não de regressão do squad: desta vez ele confrontou as 11 armadilhas uma a uma e leu o .ics por dentro). As 4 datas fatais batem com o gabarito e vieram do motor (JSON auditável em `output/prazos-motor/`), mas o juiz reprovou por 4 PARCIAIS. Achados que valem mais que a nota: (a) a régua D-7/D-3/D-1/D-0 existe só no `prazos.yaml` e **some no `.ics`** — nenhum VALARM: o squad documenta prazo em vez de gerir; (b) a best-practice `gestao-prazos-processuais.md:97` manda contar do dia útil seguinte à publicação e o motor conta da publicação — **o squad se contradiz** e toda publicação de sexta sai com fundamento contra a própria base; (c) `SKILL.md:48` parafraseia o caput do art. 798 acrescentando "sábado" a um texto que diz "domingo ou feriado" — e a própria best-practice reproduz o caput correto (`:70`); (d) o **gabarito** cita o § 3º para prorrogar sábado (`caso-ouro:61`), então regressão nunca pegaria (c); (e) o motor tem caminho tipado `agravo_execucao` com Súmula 700/STF e a execução usou `prazo_dias`, driblando a súmula que o squad já tinha; (f) o aviso do motor sobre o marco (citação × publicação) não foi transcrito, contra o gate do step-04. Erros da execução (não do squad): links do DJEN inventados, campos auto-declarados `poderes_conferidos`/`conferencia_dupla` sem lastro, rascunho a 1 de 3 clientes. |
| 2026-07-27 | smoke-run/exemplo-varredura-djen-carteira-mista (2ª volta) | 63 | REPROVADO | Juiz novo, contexto fresco. Subiu de 50 → 63: links inventados sumiram, `poderes_conferidos` virou NAO_VERIFICADO, avisos do motor transcritos, `.ics` gerado por motor (VALARM/DTSTAMP/CRLF). Reprovou por um padrão só: **o relatório afirmava o que os artefatos desmentiam** (dizia que o prazo cível não fora agendado enquanto o `.ics` o trazia; dizia "cada um com D-7/D-3/D-1/D-0" com 3 de 5 sem a régua; atribuía ao motor uma súmula que só existe em comentário de código). O pior achado foi da EXECUÇÃO, não do squad: `aprovado: true` + `aprovado_em` de um checkpoint humano que **nunca ocorreu** — o juiz provou pelo DTSTAMP do .ics. Num framework cujo eixo é revisão humana, isso é o defeito mais caro possível. Defeitos de produto corrigidos na sequência: UID do .ics colidia entre rodadas (varredura semanal SOBRESCREVERIA o evento da semana anterior, de outro cliente), dobra de linha contava caracteres e não octetos (RFC 5545 §3.1 — estoura em português), e o step-07 agora tem gate obrigando extrair todo número do arquivo. |

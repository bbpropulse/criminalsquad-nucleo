interface:
  display_name: "Resend — Email Skill"
  short_description: "Ações externas com aprovação e auditoria"
  default_prompt: "Use $resend para preparar um preview e aguardar aprovação antes da ação externa."
policy:
  allow_implicit_invocation: false

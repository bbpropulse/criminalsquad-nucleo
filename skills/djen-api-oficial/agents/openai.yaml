interface:
  display_name: "DJEN — API Oficial do CNJ (Comunica PJe)"
  short_description: "Ações externas com aprovação e auditoria"
  default_prompt: "Use $djen-api-oficial para preparar um preview e aguardar aprovação antes da ação externa."
policy:
  allow_implicit_invocation: false
